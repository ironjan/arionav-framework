package de.ironjan.arionav_fw.ionav

/** Shows that this class holds the ionav dependency container. Should be used on the app's application class or another appropriate place. */
interface IonavContainerHolder {
    val ionavContainer: IonavContainer
}